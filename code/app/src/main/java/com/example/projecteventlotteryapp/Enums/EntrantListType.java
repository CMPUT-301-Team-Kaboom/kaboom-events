package com.example.projecteventlotteryapp.Enums;

/**
 * Enum representing the different statuses or lists an entrant can belong to for a specific event.
 * */
public enum EntrantListType {
    WAITLIST,
    INVITED,
    ENROLLED,
    DECLINED
}
